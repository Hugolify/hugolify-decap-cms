import { t } from '../i18n/translater.js';

export const is_primary = {
  name: 'is_primary',
  label: t.fields.is_primary,
  widget: 'boolean',
  default: false,
  required: false,
  i18n: 'duplicate'
};
